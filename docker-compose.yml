services:
  avtorgraf:
    build: .
    container_name: avtorgraf
    env_file:
      - .env
    ports:
      - "${APP_PORT:-8080}:8080"
    volumes:
      - avtorgraf-data:/app/data
    restart: unless-stopped

volumes:
  avtorgraf-data:
